node_modules

# Output
.output
.vercel
.netlify
.wrangler
/.svelte-kit
/build
# Built story packages (scripts/build-stories.mjs) — release assets, never committed
/dist
# Same packer, `--static`: the bundled example stories the app offers for install. Generated by
# predev/prebuild from stories/, so the sources stay the single place a story is edited.
/static/stories

# Per-developer Claude Code permissions. `.claude/settings.json` is the shared one and is committed.
.claude/settings.local.json

# OS
.DS_Store
Thumbs.db

# Env
.env
.env.*
!.env.example
!.env.test

# Vite
vite.config.js.timestamp-*
vite.config.ts.timestamp-*
